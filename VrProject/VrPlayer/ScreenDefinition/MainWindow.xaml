<Window x:Class="ScreenDefinition.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
       <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Width="100" Height="30" Content="Сменить дисплей" Click="Button_Click" />
            <TextBox Name="_displayNumber" Grid.Column="1" Text="0" TextAlignment="Center" VerticalContentAlignment="Center" />
        </Grid>
     <ListBox Grid.Row="1" Name="_infoListBox" HorizontalAlignment="Stretch" Margin="0,10,0,0" VerticalAlignment="Stretch"/>

    </Grid>
</Window>
